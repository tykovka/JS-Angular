'use strict'
//1
let Ts = 42;
let Tf = (9 / 5) * Ts + 32;
alert (Tf);

//2
let name = "Василий";
let admin;
admin = name;
alert(admin);

/** 3 
 * 100108, будет происходить приведение типов данных, число 100 станет строкой
 * 
 * 4
 * async defer позволяют асинхронно загружать скрипты, т.е. не останавливают загрузку HTML-страницы. 
 * async запускает скрипты по мере их загрузки, а defer сохраняет четкий порядок запуска скриптов
 * */ 
